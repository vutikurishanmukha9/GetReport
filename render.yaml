# ═══════════════════════════════════════════════════════════════════════════════
# GetReport — Render Blueprint (Infrastructure as Code)
#
# Services:
#   1. Backend API  (FastAPI + WeasyPrint)     — Docker, Web Service
#   2. Celery Worker (background tasks)        — Docker, Background Worker
#   3. Frontend     (Vite React)               — Static Site
#   4. Redis        (Celery broker/backend)    — Render Redis
#
# External (not managed by Render):
#   - Neon DB Postgres → Set DATABASE_URL in Render Dashboard
#   - OpenAI API       → Set OPENAI_API_KEY in Render Dashboard
# ═══════════════════════════════════════════════════════════════════════════════

services:
  # ─── 1. Backend API + Worker (Single Container) ─────────────────────────────
  - type: web
    name: getreport-backend
    runtime: docker
    dockerfilePath: ./Backend/Dockerfile
    dockerContext: .
    plan: starter
    region: oregon
    healthCheckPath: /health
    # Single Service Mode: Run start.sh to launch both API and Celery
    numInstances: 1 
    envVars:
      - key: DATABASE_URL
        sync: false # Set manually → Neon DB Postgres connection string
      - key: REDIS_URL
        sync: false # Set manually → Your External Redis URL (e.g., Upstash/Neon)
      - key: OPENAI_API_KEY
        sync: false # Set manually in Render Dashboard
      - key: PDF_ENGINE
        value: weasyprint
      - key: CORS_ORIGINS
        sync: false # Set to your frontend URL
      - key: SKEWNESS_THRESHOLD
        value: "1.0"
      - key: STORAGE_TYPE
        value: local 
      - key: MAX_CONTENT_LENGTH
        value: "52428800" # 50MB
      - key: PYTHONUNBUFFERED
        value: "1"

  # ─── 3. Frontend (Static Site) ──────────────────────────────────────────────
  - type: web
    name: getreport-frontend
    runtime: static
    buildCommand: cd Frontend && npm install && npm run build
    staticPublishPath: ./Frontend/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /index.html
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false # Set to https://getreport-api.onrender.com/api
